/**
 * @file
 * Global utilities.
 *
 */
(function (Drupal) {

    'use strict';
  
    Drupal.behaviors.isleSearch = {
      attach: function (context, settings) {
  
      }
    };
  
  })(Drupal);
  